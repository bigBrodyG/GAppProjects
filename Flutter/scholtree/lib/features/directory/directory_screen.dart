import 'package:flutter/material.dart';

// Phase 4 stub
class DirectoryScreen extends StatelessWidget {
  const DirectoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('rubrica — disponibile prossimamente'),
    );
  }
}
