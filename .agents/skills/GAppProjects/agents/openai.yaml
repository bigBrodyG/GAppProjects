interface:
  display_name: "GAppProjects"
  short_description: "Repo-specific patterns and workflows for GAppProjects"
  default_prompt: "Use the GAppProjects repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true